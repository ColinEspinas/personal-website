---
import { Icon } from "astro-icon";

export interface Props {
  title: string;
}

const { title } = Astro.props as Props;
---

<section>
  <h2 class="font-display font-bold text-[32px]">{title}</h2>
  <slot></slot>
</section>
